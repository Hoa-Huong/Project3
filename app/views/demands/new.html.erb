<% provide :title, "Đăng ký nhu cầu" %>
<h1>Đăng ký nhu cầu</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <ul class="errors">
      <% @demand.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
    <%= render "form_demand" %>
  </div>
</div>
